# == Class etherpad::service::base
#
# This class is provides the default service provider, if all else
# fails. It's supposed to only be called from etherpad::service
#
class etherpad::service::base {

}
